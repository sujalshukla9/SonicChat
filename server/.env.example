# =============================================================================
# SSCHATS SERVER - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to version control!
# =============================================================================

# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=production

# Client URL (for CORS)
# -----------------------------------------------------------------------------
# Local development: http://localhost:5173
# Production: Your Vercel/Netlify frontend URL
CLIENT_URL=https://your-frontend.vercel.app

# Firebase Configuration
# -----------------------------------------------------------------------------
# Get this from Firebase Console > Project Settings
FIREBASE_PROJECT_ID=your-firebase-project-id

# Google Sheets Configuration (for analytics/feedback)
# -----------------------------------------------------------------------------
GSHEET_SPREADSHEET_ID=your-google-sheet-id

# Google Service Account Credentials
# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable the Google Sheets API (APIs & Services > Library)
# 4. Create a Service Account (IAM & Admin > Service Accounts)
# 5. Create a key (JSON format) for the service account
# 6. Share your Google Sheet with the service account email
# 7. Copy the entire JSON content here (on a single line)
# -----------------------------------------------------------------------------
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id",...}

# Cloudinary Configuration (for file/image uploads)
# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS:
# 1. Go to https://cloudinary.com and create an account
# 2. Go to Dashboard: https://cloudinary.com/console
# 3. Copy Cloud Name, API Key, and API Secret
# -----------------------------------------------------------------------------
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# JWT Configuration (if using custom auth alongside Firebase)
# -----------------------------------------------------------------------------
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_EXPIRES_IN=7d

# Rate Limiting (optional)
# -----------------------------------------------------------------------------
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
